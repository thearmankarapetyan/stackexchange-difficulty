name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install package and test tools
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e . pytest ruff

      - name: Run tests
        run: python -m pytest

      - name: Run lint
        run: python -m ruff check .

      - name: Verify console script
        run: |
          stackexchange-difficulty --help
          stackexchange-difficulty run-sede-pilot --help
          stackexchange-difficulty run-sede-comment-enrichment --help
          stackexchange-difficulty prepare-inspection --help
          stackexchange-difficulty prepare-comment-reinspection --help
          stackexchange-difficulty summarize-inspection --help
          stackexchange-difficulty summarize-comment-reinspection --help
          stackexchange-difficulty prepare-hf-release --help
          stackexchange-difficulty upload-hf-release --help
