# Pilot Audits

This directory stores tracked audit summaries for future real SEDE pilots.

Audit files must contain aggregate counts, validation outcomes, command records,
hashes, and decisions. They must not contain Stack Exchange question titles,
post bodies, answer text, code snippets, comments, or user profile content.

Raw SEDE exports and processed pilot tables remain local under `data/` and are
ignored by Git.
